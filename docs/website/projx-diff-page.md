# Website copy — base10consultants.com/tools/projx-diff

Approved copy for the tool page, ready to paste. Screenshots referenced
below live in `docs/screenshots/` (retina PNGs, entirely fictional sample
data — safe to publish). Update the version number if it ever drifts from
the latest published release.

---

## Projx Diff — see exactly what changed in your DriveWorks™ projects.

Compare any two versions of a project and get one shareable HTML report
covering variables, rules, forms, calculation and lookup tables, macros,
documents, and navigation — with inline formula diffs, search, and filters.
Free, runs entirely on your machine, no Python required.

*[screenshot: docs/screenshots/report.png — alt: "Projx Diff comparison
report showing added, modified, and unchanged variables with inline formula
diffs"]*

**New in 1.5: a redesigned report, in light and dark.** Diff totals and a
per-section navigator with live change counts sit in a left sidebar, so
even a large diff reads at a glance. Auto/Light/Dark themes follow your OS
or your explicit choice. Status colors are colorblind-safe by design, and
every change is labeled — never color alone. Captured model and model-rule
changes now appear in the report too.

**Track your whole project library, automatically.** Point
Projx Diff at your project shares and it archives every project nightly
into version control, detects what changed, and builds a team dashboard —
activity trends, most-active projects, and per-user contribution,
attributed automatically to whoever last saved each project in DriveWorks.
Multiple locations (production, staging, satellite offices) roll into one
dashboard with per-location views, while their histories stay safely
separated. New projects and unfamiliar user names are flagged for one-click
triage right in the app. Scriptable JSON output feeds Power BI or your own
reporting.

*[screenshot: docs/screenshots/dashboard.png — alt: "Projx Diff work
dashboard with activity chart, top projects, per-user totals, and
production/staging tabs"]*

**Download:** Windows installer (recommended — Start Menu entry and clean
uninstall), portable Windows .exe, macOS app, or Linux binary for headless
servers. All free.

Projx Diff is an independent tool from Base 10 Consultants and is not
affiliated with, endorsed by, or tested by DriveWorks™ Ltd.

---

Download links (per release): https://github.com/CarbonNapkin/ProjxDiff/releases/latest

- `ProjxDiff-setup.exe` — Windows installer
- `ProjxDiff.exe` — Windows portable
- `ProjxDiff-macos.zip` — macOS app
- `ProjxDiff-linux` — Linux binary
